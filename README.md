# Seasoned Syndicate Sheet Dashboard

A minimal read-only website for viewing the published Google Sheet as a cleaner searchable dashboard.

## Run locally

```bash
npm install
npm run dev
```

## Build for deployment

```bash
npm run build
```

The Google Sheet source is configured in `src/main.jsx` with `PUBLISHED_SHEET_URL` and `CSV_URL`.
